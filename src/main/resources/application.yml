logging:
  level:
    com:
      userservice: DEBUG
    root: INFO
management:
  endpoints:
    web:
      exposure:
        include: '*'
server:
  port: 9098
spring:
  data:
    mongodb:
      database: test
      host: localhost
      port: 27017
jwt:
  secret: secretKey
